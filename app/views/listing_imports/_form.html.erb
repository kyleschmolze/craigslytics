<%= form_for(@listing_import) do |f| %>
  <% if @listing_import.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@listing_import.errors.count, "error") %> prohibited this listing importer from being saved:</h2>

      <ul>
      <% @listing_import.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
