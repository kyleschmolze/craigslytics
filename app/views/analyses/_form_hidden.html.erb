<%= form_for(@analysis)  do |f| %>
  <% if @analysis.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@analysis.errors.count, "error") %> prohibited this analysis from being saved:</h2>

      <ul>
      <% @analysis.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.hidden_field :address, placeholder:"Address" %>
    <%= f.hidden_field :bedrooms, placeholder:"Number of Bedrooms" %>
 
    <%= link_to raw("<i class='icon-arrow-left'></i> That's not it, try again."), url_for(controller: 'analyses', action:'home'), class: "btn"  %>
    <%= f.submit "That's the one, continue!", class: "btn btn-primary" %>
<% end %>
